export { auth as proxy } from "@/lib/auth";

export const config = {
  matcher: ["/dashboard"],
};

// import { auth } from "@/lib/auth";
// export const middleware = auth;

// export const config = {
//   matcher: ["/dashboard"],
// };
